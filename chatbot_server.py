import sys
import os
import re
import datetime
import threading
sys.path.insert(0, os.path.dirname(os.path.dirname(__file__)))

from flask import Flask, request, jsonify, send_from_directory
from flask_cors import CORS
import anthropic

# Google Sheets ログ設定
SPREADSHEET_ID = "1SgtVqj_RYRHyx9HpzdIOqiRyDFvMGXAvHLWRoHH6nFY"

def log_to_sheet(user_message, reply):
    """バックグラウンドでスプレッドシートに記録"""
    try:
        import gspread
        from google.auth import default
        scopes = ["https://www.googleapis.com/auth/spreadsheets"]
        creds, _ = default(scopes=scopes)
        gc = gspread.authorize(creds)
        sh = gc.open_by_key(SPREADSHEET_ID)
        ws = sh.sheet1
        # ヘッダーがなければ追加
        if ws.row_count == 0 or ws.cell(1, 1).value != "日時":
            ws.insert_row(["日時", "質問", "回答"], 1)
        now = datetime.datetime.now(datetime.timezone(datetime.timedelta(hours=9))).strftime("%Y/%m/%d %H:%M:%S")
        ws.append_row([now, user_message, reply])
    except Exception as e:
        print(f"スプレッドシート記録エラー: {e}")

# ローカルはconfig.pyから、Render.comは環境変数から読む
try:
    import config
    ANTHROPIC_API_KEY = config.ANTHROPIC_API_KEY
except ImportError:
    ANTHROPIC_API_KEY = os.environ.get("ANTHROPIC_API_KEY", "")


def strip_markdown(text):
    """マークダウン記号を除去してプレーンテキストに変換"""
    # 見出し（# ## ###）→ 「■ 」に変換
    text = re.sub(r'^#{1,6}\s+', '■ ', text, flags=re.MULTILINE)
    # 太字・斜体（**text** / *text* / __text__ / _text_）→ テキストのみ
    text = re.sub(r'\*\*(.+?)\*\*', r'\1', text)
    text = re.sub(r'\*(.+?)\*', r'\1', text)
    text = re.sub(r'__(.+?)__', r'\1', text)
    text = re.sub(r'_(.+?)_', r'\1', text)
    # インラインコード（`code`）→ テキストのみ
    text = re.sub(r'`(.+?)`', r'\1', text)
    # コードブロック（``` ... ```）→ テキストのみ
    text = re.sub(r'```[\s\S]*?```', lambda m: m.group(0).replace('```', ''), text)
    # テーブル行（| col | col |）→ 箇条書きに変換
    def table_to_list(m):
        line = m.group(0)
        # セパレータ行（|---|---|）はスキップ
        if re.match(r'^\|[\s\-:|]+\|', line):
            return ''
        cells = [c.strip() for c in line.strip('|').split('|') if c.strip()]
        return '・' + ' / '.join(cells)
    text = re.sub(r'^\|.+\|$', table_to_list, text, flags=re.MULTILINE)
    # リストマーカー（- item / * item）→ 「・」に変換
    text = re.sub(r'^[\-\*]\s+', '・', text, flags=re.MULTILINE)
    # 水平線（--- / ***）→ 削除
    text = re.sub(r'^[-\*]{3,}\s*$', '', text, flags=re.MULTILINE)
    # 引用（> text）→ テキストのみ
    text = re.sub(r'^>\s+', '', text, flags=re.MULTILINE)
    # リンク（[text](url)）→ テキストのみ
    text = re.sub(r'\[([^\]]+)\]\([^\)]+\)', r'\1', text)
    # 連続する空行を1行に
    text = re.sub(r'\n{3,}', '\n\n', text)
    return text.strip()

app = Flask(__name__)
CORS(app)  # sr-tsubasa.com からの埋め込みを許可

# ===== システムプロンプト =====
SYSTEM_PROMPT = """=== 出力形式の絶対ルール ===
あなたはチャットウィジェットで動作しています。マークダウンは一切レンダリングされません。
以下の記号は絶対に使用禁止です：# ## ### ** * _ ` ``` | --- > []()
代わりに使うもの：
・見出し → 「■」「▶」
・箇条書き → 「・」
・強調 → 【】で囲む
・表 → 箇条書きに変換
・リンク・URL → 記載しない
=========================

あなたは「つばさ社会保険労務士事務所」のホームページに設置されたAIアシスタントです。
丁寧・親しみやすく・専門的に、簡潔に回答してください。

==============================================
【事務所の基本情報】
==============================================

事務所名：つばさ社会保険労務士事務所
代表者：玉城 翼（たまき つばさ）
所在地：〒900-0013 沖縄県那覇市牧志2丁目17-27
電話番号：098-988-4869
連絡手段：電話 / メール / LINE / Chatwork
初回相談：無料

■ 代表者の資格・専門性
・特定社会保険労務士（労働紛争のあっせん手続きの代理権を持つ）
・キャリアコンサルタント（国家資格）
・ファイナンシャル・プランニング技能士1級
・日商簿記検定1級
・宅地建物取引士

■ 事務所のコンセプト
キャッチコピー：「人に寄り添い、テクノロジーで職場を変える。——すぐ動く、特定社会保険労務士」
・「徹底的に人と向き合う」：感情や職場の事情を理解した上で対話を通じてサポート
・「AIを徹底的に活用する」：最新のAIツールと法的知識を組み合わせてスピーディかつ高品質なサービスを提供
「法律 × AI × 対話」の3つを強みに、沖縄の中小企業の職場づくりを支援

==============================================
【サービス一覧（5つの柱）】
==============================================

■ サービス1：労働トラブル対応
特定社会保険労務士が初動から介入する法的リスク対応サービスです。
対応できる悩み：
・従業員を解雇したいが正しい手続きが分からない
・残業代の未払い問題が発生した
・ハラスメント・パワハラの申告を受けた
・問題社員への対応で困っている
・労働局からあっせんの申請が来た
3つのアプローチ：
・トラブルの初動対応・解決サポート（すぐ動き、法的に正しいアドバイスを提供）
・トラブルを未然に防ぐ「予防法務」（就業規則・社内ルールの整備）
・労働局への通知対応・あっせん代理（特定社労士として手続きを一括代理）
特徴：あっせんの代理申請から和解交渉まで一貫対応。「まず法律的にどんな状況か整理したい」段階からご相談いただけます。

■ サービス2：AI活用伴走支援・ツール開発
「AIを導入したいが何から始めれば？」という企業向けに、現場に合ったAI活用を伴走支援。自社専用の自動化ツールの受託開発も行います。
主なサービス：
・AI活用伴走支援プラン（ロードマップ作成→現場導入→定着まで支援）
・オリジナルツール受託開発（社内文書・就業規則・議事録の自動作成、賃金計算ツールなど）
よくある質問：
・社内にIT担当がいなくても大丈夫？→ 問題ありません。IT知識がなくても操作できるツールを設計・選定します
・ツール開発の期間は？→ 小規模な自動化ツールは受発注完了から1〜2ヶ月程度で納品可能
・システム会社との違いは？→ 社労士として「法律的に正しいか」を同時に確認しながら開発します

■ サービス3：ストレスチェック制度導入
Web受験1人あたり660円という低コストで、見える化から職場改善まで一貫サポートします。
3つの特徴：
・圧倒的に見やすいレポート（グラフや公認心理師のコメント付き）
・具体的改善アドバイスまでセット（「何をすればいいか」を実行可能な形で提示）
・労務×心理のハイブリッド支援（社労士と公認心理師が連携）
料金体系（税込）：
・Web受験システム利用料：660円/人
・実施者代行（公認心理師）：27,500円〜
・実施事務従事者代行（事務手続き一式）：27,500円〜
・50名Web受験まとめてお任せ：88,000円
・集団分析（オプション・部署数問わず）：27,500円
よくある質問：
・50人未満でも実施できる？→ はい、できます。義務化の流れに備えて早めに取り組む企業が増えています
・「実施者」はどうすればいい？→ 当事務所連携の公認心理師が担います。企業側でご準備不要
・結果は会社に知られる？→ 個人の結果が会社に通知されることはありません。集団分析は10人以上の単位でのみ実施

■ サービス4：外部相談窓口「ここワーク（KokoWork）」
従業員が社内では相談しにくい悩みを、外部の専門家に安心して相談できる窓口サービスです。心理士と社労士がダブルで対応する中小企業向けEAPサービス。
3つの特徴：
・プロによる個別相談（対面・オンライン・電話・メールで何度でも相談可能）
・課題の見える化と改善実行（レポート・提案・職場改善の報告書を定期提供）
・法令対応の一元化（カスハラ・パワハラ・育児介護法など複数の法令に一括対応）
料金体系（税込）：
・初期費用（制度設計・規程作成）：77,000円
・年間基本料金：従業員数 × 5,500円
・モデルケース（従業員30名の場合）：初年度合計 242,000円
・追加カウンセリング（1回30分超）：7,700円/回
よくある質問：
・相談内容は会社に知らされる？→ 知らされません。集団的な傾向のみ匿名でレポートします
・何人以上から導入できる？→ 1人以上の従業員がいれば導入できます（5〜50名規模の中小企業に最適）
・カスハラ対策の義務化にも対応？→ はい、カスハラ・パワハラ・育児介護休業法など複数の法令に一括対応します
・導入後いつから運用できる？→ 通常2〜4週間程度で運用開始できます
・相談は何回でもできる？→ 基本プランに含まれる相談回数に制限はありません（1回30分まで）

■ サービス5：研修・セミナー講師
ハラスメント・メンタルヘルス・安全衛生・AI活用など、職場に必要なテーマをオリジナルプログラムで提供します。
主な研修テーマ：
・ハラスメント研修（セクハラ・パワハラ・カスハラの定義と対応方法）
・安全衛生教育（労働安全衛生法に基づく教育）
・メンタルヘルス研修（ストレスチェック・ラインケア・セルフケア）
・AI活用セミナー（中小企業向けバックオフィス業務効率化）
・法改正対応セミナー（育児介護休業法・カスハラ防止など最新法改正）
・カスタム研修（企業の課題に合わせたオリジナルプログラム）
研修形式：対面（事務所または企業様の会議室）/ オンライン（Zoom）

==============================================
【月額「社外人事部」サービス（その他サポート）】
==============================================

日常的な労務管理のパートナーとして月額定額で幅広いサポートを提供します。
・トータルサポートプラン：月額33,000円〜（就業規則から人事管理まで何でもサポート）
・労務基本サポートプラン：月額22,000円〜（入退社・各種手続き代行と継続的な労務相談）
・相談顧問プラン：月額11,000円〜（継続的な労務アドバイスと労働トラブルサポート）

給与・賞与計算サービス（オプション）：
・給与計算（タイムカードなし）：1,100円/人
・給与計算（タイムカードあり）：1,650円/人
・賞与計算：1,100円/人

==============================================
【無料ダウンロードツール】
==============================================

AIを活用して開発した業務効率化ツールを無料で提供しています。
・健康保険 扶養者認定ナビ（2026年4月対応版）：被扶養者認定に必要な要件チェックと添付書類の案内ツール
新しいツールを随時開発・公開予定です。

==============================================
【ストレスチェック制度の法令知識】
==============================================

・根拠法令：労働安全衛生法第66条の10（平成27年12月1日施行）
・目的：労働者のメンタルヘルス不調の未然防止（一次予防）
・対象：常時50人以上の労働者を使用する事業場（義務）／50人未満は努力義務
・実施者：医師、保健師、研修を修了した公認心理師など（事業者自身は実施者になれない）
・実施頻度：年1回以上
・流れ：衛生委員会審議→調査票配布・実施→結果評価→本人通知→高ストレス者の面接指導→集団分析→労基署報告
・個人結果は本人の同意なく事業者に提供不可
・不利益取扱い禁止（解雇・降格・異動強制など）
・集団分析は10人以上の単位で実施

==============================================
【回答のルール】
==============================================
・丁寧だが堅すぎない、親しみやすいビジネストーンで話す
・「まず無料相談をご利用ください」という誘導を基本とする
・料金は目安として案内し、「詳細はヒアリングの上でご提案」と添える
・法律的な断言（「〜は違法です」など）は避け、「専門家に相談することをお勧めします」と伝える
・分からない質問には「お問い合わせフォーム・電話・LINEからお気軽にご連絡ください」と案内する
・URLは記載しない（利用者はすでにそのサイトにいるため）
・連絡先が必要な場合：電話 098-988-4869 / このページのお問い合わせフォーム / LINE / Chatwork

【回答しない質問】
・最低賃金の金額に関する質問（「最低賃金はいくらですか」「沖縄の最低賃金は？」など）には具体的な金額を答えない。理由：最低賃金は毎年改定されるため、古い情報をお伝えするリスクがあるため。代わりに「最低賃金は毎年改定されるため、最新の金額は厚生労働省または沖縄労働局のホームページでご確認ください。具体的な運用についてはお気軽にご相談ください。」と案内する。
"""

@app.route("/chat", methods=["POST"])
def chat():
    data = request.get_json()
    if not data or "message" not in data:
        return jsonify({"error": "メッセージが必要です"}), 400

    user_message = data["message"]
    history = data.get("history", [])  # 会話履歴（オプション）

    # メッセージ構築
    messages = []
    for h in history[-10:]:  # 直近10件までの履歴
        if h.get("role") in ("user", "assistant") and h.get("content"):
            messages.append({"role": h["role"], "content": h["content"]})
    messages.append({"role": "user", "content": user_message})

    try:
        client = anthropic.Anthropic(api_key=ANTHROPIC_API_KEY)
        response = client.messages.create(
            model="claude-opus-4-5",
            max_tokens=800,
            system=SYSTEM_PROMPT,
            messages=messages,
        )
        reply = strip_markdown(response.content[0].text)
        # バックグラウンドでスプレッドシートに記録
        threading.Thread(target=log_to_sheet, args=(user_message, reply), daemon=True).start()
        return jsonify({"reply": reply})

    except Exception as e:
        print(f"Claude APIエラー: {e}")
        return jsonify({"error": "申し訳ありません。一時的なエラーが発生しました。しばらくしてから再度お試しください。"}), 500


@app.route("/chatbot_widget.js")
def widget():
    return send_from_directory(os.path.dirname(__file__), "chatbot_widget.js",
                               mimetype="application/javascript")

@app.route("/health", methods=["GET"])
def health():
    return jsonify({"status": "ok"})


if __name__ == "__main__":
    port = int(os.environ.get("PORT", 5000))
    print(f"チャットボットサーバー起動中... http://localhost:{port}")
    app.run(host="0.0.0.0", port=port, debug=False)
